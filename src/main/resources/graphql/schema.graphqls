type Query {
    user(id: ID!): User
    users: [User!]
}

"""
ユーザー
"""
type User {
    id: ID!

    """
    ユーザー名
    """
    name: String!

    """
    タスク一覧
    """
    todos: [ToDo!]
}

"""
タスク
"""
type ToDo {
    id: ID!

    """
    説明
    """
    summary: String!
}